pages:
  stage: deploy
  script:
    - rm -rf public
    - mkdir public
    - cp index.html public/
    - cp styles.css public/
    - cp main.js public/
  artifacts:
    paths:
      - public
  only:
    - main
